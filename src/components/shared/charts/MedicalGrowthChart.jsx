import { ResponsiveContainer,LineChart,Line,CartesianGrid,XAxis,YAxis,Tooltip,Legend } from "recharts";
import ChartCard from "./ChartCard.jsx";
export default function MedicalGrowthChart({data=[],loading}){return <ChartCard title="প্ল্যাটফর্ম গ্রোথ" description="নতুন ইউজার, ডাক্তার, অ্যাপয়েন্টমেন্ট ও রক্তের অনুরোধ" loading={loading} isEmpty={!loading&&!data.length} height={300}><ResponsiveContainer width="100%" height="100%"><LineChart data={data} margin={{top:8,right:8,left:-20,bottom:0}}><CartesianGrid strokeDasharray="3 3" stroke="hsl(var(--border))"/><XAxis dataKey="date" tick={{fontSize:10}} tickFormatter={v=>String(v).slice(5)}/><YAxis allowDecimals={false} tick={{fontSize:10}}/><Tooltip/><Legend wrapperStyle={{fontSize:11}}/><Line type="monotone" dataKey="new_users" name="নতুন ইউজার" stroke="hsl(var(--primary))" strokeWidth={2} dot={false}/><Line type="monotone" dataKey="new_doctors" name="নতুন ডাক্তার" stroke="hsl(var(--accent))" strokeWidth={2} dot={false}/><Line type="monotone" dataKey="appointments" name="অ্যাপয়েন্টমেন্ট" stroke="hsl(var(--foreground))" strokeWidth={2} dot={false}/><Line type="monotone" dataKey="blood_requests" name="রক্তের অনুরোধ" stroke="hsl(var(--destructive))" strokeWidth={2} dot={false}/></LineChart></ResponsiveContainer></ChartCard>}
