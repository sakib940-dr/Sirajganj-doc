-- Doctor Platform V1: appointment UI support.
-- The consolidated Step-2 SQL already creates appointments, triggers, RLS and indexes.
-- This migration is intentionally a no-op so the existing production schema is not changed.
-- Use the Step-2 consolidated SQL for a fresh database.
select 1;
